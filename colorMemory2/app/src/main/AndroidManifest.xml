<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.colormemory2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_simon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_simon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ColorMemory2">
        <activity android:name=".ModeDifficile"></activity>
        <activity android:name=".ModeFacile" />
        <activity android:name=".Ranked" />
        <activity android:name=".How_play" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Menu" />
        <activity android:name=".Connection" />
        <activity android:name=".Register" />
    </application>

</manifest>